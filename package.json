{
  "name": "Yrucomi",
  "description": "",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "test": "lerna run test",
    "bootstrap": "yarn clean && lerna bootstrap && yarn build",
    "clean": "lerna clean --yes",
    "lint": "lerna run lint",
    "type-check": "lerna run type-check",
    "build": "lerna run build"
  },
  "devDependencies": {
		"@babel/cli":"^7.1.2",
		"@babel/core":"^7.1.2",
    "lerna": "^2.11.0",
		"prettier": "^1.14.3",
		"typescript":"^3.1.3"
  }
}
